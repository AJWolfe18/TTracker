# ADO-438/440 Session — Gold Set Fix, Board Cleanup, Pilot Batch, Reconciliation Discovery

## What happened
- Gold set Case 131 corrected ("vacated" → "reversed") with audit trail in `docs/features/scotus-qa/gold-set-changelog.json`. Validation: 132/145 (91.0%).
- ADO-438 description rewritten with rollback instructions, moved to **Ready for Prod**.
- ADO-429 moved to **Ready for Prod** (all AC confirmed).
- Closed: 393 (eval baseline), 394 (prompt remediation — 7/9 AC met, 2 technicalities documented), 395 (superseded by 438+440), 439 (validation complete).
- Created: ADO-444 (disposition + Oyez fix, P3), ADO-445 (distribution sanity check, P3), ADO-446 (reconciliation step, **P1 blocker**).
- All cards 438–446 parented to Feature 392 (SCOTUS Eval & Remediation).
- ADO-446 linked as predecessor to ADO-440.

## Pilot batch results (10 cases)
Ran v9 pipeline on 10 cases. **3 clean, 4 blocked, 1 flag, 2 failed enrichment.** Cost: $0.49.

- **Clean:** Delligatti (127), Smith & Wesson (177), Skrmetti (204), FSC v. Paxton (238)
- **Blocked:** Trump v. J.G.G. (143) — missing dissenters; Ames (179) — merits misclassified as procedural; FCC (236) — wrong vote split + missing dissenters; Mahmoud (237) — missing dissenters
- **Failed:** Starbucks (12) — validator name-matching bug; Campos-Chaves (27) — Pass 1 can't extract evidence

## Systemic issues found → ADO-446
1. GPT misses dissenters on split-vote cases (dissent_authors=[] on 6-3 and 5-4 cases)
2. "Vacated and remanded" auto-classified as procedural even when it's a merits ruling
3. SCOTUSblog docket mismatch used instead of rejected
4. Validator string matching bug (first-name vs full-name)

**Root cause:** No cross-check between Pass 1 (GPT) and SCOTUSblog grounding data. Fix = reconciliation step (ADO-446).

## Next session
1. **ADO-446** — Build reconciliation step. Plan should be created at session start.
2. Re-run pilot 10 cases after fix — target 8/8 pass full 11-field validation.
3. Then ADO-440 bulk run is unblocked.

## Files changed
- `tests/scotus-gold-truth.json` — Case 131 disposition fix
- `docs/features/scotus-qa/gold-set-changelog.json` — new (audit trail)
- `docs/handoffs/2026-03-09-ado438-testing.md` — this file
