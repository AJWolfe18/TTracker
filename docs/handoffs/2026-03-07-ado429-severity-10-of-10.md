# 2026-03-07 — ADO-429: Severity Calibration — 10/10 Gold Pass

## What Happened
Fixed the three-system conflict on severity: reversed the label→level mapping (level is now source of truth, label derived from it), added deterministic severity bounds (`computeSeverityBounds()`) based on dissent, issue_area, and holding text content, and fixed false-positive `explicitOverrule` regex. Gold set eval now passes **10/10** (up from 5/10). See `docs/features/scotus-enrichment/severity-changelog.md` for full details.

## Next Session
- Verify AC via `/ado` for ADO-429 — severity within range for all 10 gold cases is now MET
- Move ADO-429 to Testing
- Monitor: issue_area extraction is non-deterministic (GPT gives different values per run) — severity bounds use DB + Pass 1 + holding text as multi-signal detection to compensate
- Separate work: variation pool misassignment (e.g., "grudging_credit" for non-people-win cases like Lackey) — not blocking but worth investigating
