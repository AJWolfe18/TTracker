# Session Handoff — 2026-08-17: ADO-530 closed, Fronts Wave 1 broken out, W1.1 rap sheet timeline built

## What happened

Josh opened with a product gut-check: "I want an easy-to-find list of all the bullshit — a rap sheet
of all the crazy shit, not just the fronts. Is the Fronts path that, or do we need something simpler?"

**Answer: the rap sheet was already in the PRD** (the homepage timeline §5.1 + running log §5.2 —
both render straight from existing tables with zero fronts). What his instinct exposed was a
*sequencing* problem: Wave 1 as written led with the front registry. Two decisions locked:

1. **Rap-sheet-first resequencing.** Wave 1 now builds: (1) rap sheet surfaces → (2) front layer
   (migrations, registry, assignment, detail pages) → (3) GA4 instrumentation. PRD §11 updated.
2. **One front per story — confirmed.** Hard PK on `story_event.story_id` (PRD §12 Q2 resolved).
   Relaxing later is a migration; Josh accepted that trade explicitly.

Remaining §12 questions (naming, domain-page timelines, expand interactivity, label wording,
baseline-only targets) proceed on the PRD's recommended defaults.

## ADO state

- **ADO-530** (design story) → **Closed**. All 3 ACs met by the PRD; closing comment documents both decisions.
- **ADO-543** (new Epic, "Fronts — the rap sheet timeline + containment layer") → Active.
- **ADO-544–549** (Wave 1 stories W1.1–W1.6, each with full ACs) created under 543:
  544 rap sheet timeline · 545 expand + running log · 546 migrations · 547 admin registry ·
  548 front pages · 549 GA4 + allowlist.
- **ADO-544** → **Testing** (built this session, deployed to test branch).

## What was built (ADO-544, W1.1)

| File | What |
|---|---|
| `src/lib/timeline.ts` | `TimelineEntry` type, per-source row adapters (stories/EO/SCOTUS/pardons), `mergeEntries`, `fetchTimelineEntries` — 4 parallel PostgREST fetches with **tight selects** (no content/embedding; ~135 rows total). Recent window only: 60 stories + 25 each other source; full-term view is W1.2's expand. |
| `src/hooks/useFeatureFlag.ts` | React-side reader for `public/shared/flags-{test,prod}.json` + `?ff_name=true` URL override (same contract as vanilla `feature-flags.js`). |
| `src/components/RapSheetTimeline.tsx` | The strip: evenly spaced labeled entries (date + headline + source + alarm), alternating above/below a dotted line, month/year dividers, alarm-colored dots (hollow for non-story sources), nav arrows, source filter chips, parked at today. **<720px:** vertical single-column list, newest first, line on left. Theme-token styling, both light/dark modes inherit. |
| `src/pages/Home.tsx` | Renders the timeline on the **stories tab only**, above the hero. |
| `public/shared/flags-*.json` | New flag `rap_sheet`: **true on TEST, false on PROD**. |
| `src/__tests__/timeline.test.ts` | 11 unit tests (adapters, merge/sort/filter, routes). |
| `src/pages/Detail.tsx` | Drive-by fix for a **pre-existing** `npm run lint` failure (TS narrowing of `item` inside nested share functions — captured post-guard `const story`). |

Design source: approved concept-4 mockup (`.superpowers/brainstorm/events-homepage-v2/concept-4-hybrid.html`),
translated to app tokens (`useTheme`, `alarmPalette`).

## Verification (all green)

- `npm run lint` (tsc) — clean, including the pre-existing Detail.tsx break now fixed
- `npm run test:ui` — 115/115 (9 files) including 11 new timeline tests
- `npm run build` — clean
- `npm run qa:smoke` — exit 0
- **Live browser check** (Vite dev, localhost:3000, TEST DB): strip renders with 135 entries,
  chips toggle sources (verified stories-off leaves only scotus/eos/pardons), entry click navigates
  to `/detail/16970` correctly, no console errors, mobile branch renders all 135 items at uniform
  height. **AC4 caveat:** mobile verified at 697px viewport (same `<720px` branch); exact-360px
  eyeball is Josh's S9+ check — that's why 544 sits in Testing, not further.

## Gotchas discovered

- **Vitest is node-env**: `src/lib/supabase.ts` touches `window.location` at import time. Any
  `src/lib` module that unit tests import must lazy-import it (`await import('./supabase')`).
  Pattern set in `timeline.ts`; recorded in memory conventions.
- TEST data's newest enriched story is ~Jul 1 (enrichment down — OpenAI credits): the strip
  correctly parks there. Once credits are added and enrichment recovers, today's entries appear.

## Open items / next session

1. **Josh:** add OpenAI credits (platform.openai.com) — RSS PROD enrichment failing every 2h AND
   PR ai-review check fails until then (carried from ADO-542).
2. **Josh:** UX pass on the TEST site timeline (incl. S9+), rapid-fire feedback → iterate.
3. Then **ADO-545 (W1.2)**: full-screen expand (stacked rows) + running log section.
4. Copy note: inline strip's sub-caption says "Recent developments across every tracker, in order"
   (honest about the recent-window fetch) rather than the PRD's "Every development since
   inauguration" — that string belongs to the full-term expand view in W1.2. Flag if Josh disagrees.
