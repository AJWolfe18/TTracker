# Handoff: autonomous batch - valence fix to main, ADO-572 draft queue, ADO-577 alerts, 579/576 analysis, SCOTUS re-run review (September 3, 2026)

Josh asked for "pick up where we left off, as many cards as fit, no check-ins until the end." Seven items from the August 31 pickup list were worked; the ones that need Josh's decision are parked on their cards with a recommendation.

## Shipped to PROD

| PR | What |
|----|------|
| #141 (squash) | SCOTUS prompt: `ruling_impact_level` defined once as **valence** (damage to democracy/rights), never magnitude. Gold examples recalibrated: Barrett 2 to 1, LabCorp 1 to 2. Also carried to main the `gvr` lowercase normalization main had lost and dropped the duplicated pardons Step A.2. |

**Gotcha found:** test was AHEAD of main on both prompt files, not behind. An initial "sync test from main" went the wrong way and was undone before push. Always diff `origin/main..refs/heads/test` on `docs/features/*/prompt-*.md` before touching either side.

## Shipped to TEST (Testing in ADO)

### ADO-572 - social draft queue (commits 038498a, d605112)
- `migrations/114_social_posts.sql` **applied on TEST** through the SQL editor via claude-in-chrome (Run + "Run query" dialog both passed the auto-mode classifier). Per-type watermarks `draft_watermark_story|eo|pardon` seeded to migration `now()` so nothing historical gets drafted; `--since <iso>` is the deliberate backfill.
- `scripts/social/draft-posts.js` + `lib/copy.mjs` + `lib/select-candidates.mjs`. Verified live on TEST: 23 drafts from `--since 2026-04-10` (10 stories, 7 EOs, 6 pardons), re-run created 0 and wrote 23 `pipeline_skips` rows (`social_draft/already_drafted`). Watermarks then reset to the seed. Story candidates read `stories.main_line` directly because `v_tracker_stories` does not expose `summary_spicy`/`source_count`.
- `supabase/functions/admin-social` deployed to TEST (401 without password verified). `public/admin.html` Social tab: queue (draft + failed) with card thumbnail, editable copy, Approve / Reject; approved list with Back to draft; posted last 30 days collapsed. JSX syntax-checked with `@babel/parser` (no build step covers admin.html).
- `qa:social` (3 suites) added to `qa:smoke`.
- **Josh to verify:** TEST admin, Social tab: approve one, reject one, edit then approve, reload. AC 4 (real Discord embed) fires only in CI with the webhook secret; unit-tested until then. Scheduling of the drafter is ADO-573's poster workflow.

### ADO-577 - Discord alerts on new work (commit 884afe8)
- `scripts/lib/discord.js` shared helper (never throws, silent without `DISCORD_WEBHOOK_URL`).
- SCOTUS fetch posts only for rows INSERTED this run (`newCases[]`), pardons on `stats.inserted`, EO tracker in `saveToSupabase`. Refresh-only and quiet runs stay silent.
- `scripts/monitoring/alert-needs-review.js --domain scotus|eo|pardons` is the `if: always()` last step of the three fetch workflows; names `needs_manual_review` / `needs_review` rows from the last 26h with the reason and the admin tab. Validated against TEST (scotus 2 flagged historically, eo/pardons 0). YAML validated with the `yaml` package (`js-yaml` is not installed).
- `qa:alerts` added to `qa:smoke`. Full `npm run qa:smoke` green.

## Analysis posted on cards (Josh decides)

- **ADO-579 (Active):** PROD has 271 published EOs, all v1.1: L1 29 / L2 112 / L3 83 / L4 41 / L5 6. Josh's flag EO 14420 (Gold Standard Childhood Vaccine Recommendations) is L4, proposed 5; sibling 14407 L3, proposed 4; nine more candidates listed with reasons (14155 WHO withdrawal, 14149 "censorship", 14221/14212 MAHA, 14291, 14382/14380, 14385, 14214). Health_care is the lowest-rated category (14 of 20 at L2 or below) and where the false-premise blind spot concentrates. **Rubric change drafted on test only** (commit 1c356c0, `docs/features/eo-claude-agent/prompt-v1.md`): a false-premise rule - an EO justified by claims the record contradicts is rated on the harm those claims license; false premise is an upgrade path at 3 and 4; claim + contradicting record must appear in `section_reality_check`. Cherry-pick to main only after Josh approves; then reset + re-run the flagged subset (ADO-553 null-fields pattern).
- **ADO-576 (Active):** traced every reader of `stories.status`. `closed` would hide a story from the feed, the Tracker, share cards / OG tags, social drafts, enrichment and the admin count, and would not touch clustering (keys off `lifecycle_state`). **Recommendation: closing stays dead**; Close 576 and 210 on Josh's yes. Loose end noted: `update_story_lifecycle_states()` is only called by `scripts/rss/lifecycle.js`, which the inline tracker does not import, so `stale` is never set and the stale-reopen path is dormant (harmless today).
- **ADO-580 (New, created):** `/scotus-review latest` on the September 1 re-run. Em dashes in 8 of 13 outputs despite the tone binding (recommend a DB trigger that rewrites dashes on editorial columns + backfill); the agent reads the 15K `opinion_excerpt` rather than the full `scotus_opinions` text, which is why 2399 (ballroom) and 2099 (Slaughter) came back low-confidence with truncated authorship; 2392 has 6-3 with only two dissenters named (Aug 26 run said 7-2); 2099 has 6-3 with no dissenters. Valence check: the 0s (2108, 2094, 2095, 2100) and the 4s/5 read right; 2392 Trump v. California at 3 arguably a 4.
- **2399 ballroom case:** self-resolved. CourtListener text landed, the agent enriched it (L3, 8-1 estimated, `needs_manual_review=true`), and it is public. No `is_public=false` needed any more; it is on the 580 list for the authorship gap.

## Not done (deliberately)
- **SCOTUS routine cron** `0 16,23 * * *` on `trig_019eD3JTVeSajL4qTJJSC6tq`: still never confirmed by Josh. Ask.
- **Wins-on-timeline green styling:** live-iterate item, skipped in a no-check-in session.
- **ADO-575:** waits on Josh's one PROD Page post + screenshot.
- **ADO-564 north-star numbers:** due September 7, data not complete yet.
- `.agents/skills/` untracked dir and `exec brief for blueprint.png` at repo root: still untracked, Josh's call.
- The claude-in-chrome tab on the TEST SQL editor may still be open (close timed out).

## Noticed
- Supabase dashboard shows **EXCEEDING USAGE LIMITS** on the org (the known PROD DB size issue from May 31). Still no ticket.
- Two of the 23 TEST drafts open with "They actually fucking did it." - that is the level-5 opener from enrichment, surfaced by the first-sentence rule. Josh edits in the queue; if it repeats on PROD, consider skipping a first sentence under 30 chars.

## ADO helper used this session (REST, ~0 context)
```bash
ADO_ORG="https://dev.azure.com/AJWolfe92"; PAT=$(ado_pat)   # ado_pat() from .claude/skills/ado/SKILL.md
# state
curl -s -X PATCH -u ":$PAT" -H "Content-Type: application/json-patch+json" \
  -d '[{"op":"add","path":"/fields/System.State","value":"Testing"}]' "$ADO_ORG/TTracker/_apis/wit/workitems/<id>?api-version=7.1"
# comment (HTML)
jq -n --arg t "<p>...</p>" '{text:$t}' | curl -s -X POST -u ":$PAT" -H "Content-Type: application/json" -d @- \
  "$ADO_ORG/TTracker/_apis/wit/workItems/<id>/comments?api-version=7.1-preview.4"
# create
curl -s -X POST -u ":$PAT" -H "Content-Type: application/json-patch+json" -d '[{"op":"add","path":"/fields/System.Title","value":"..."}]' \
  "$ADO_ORG/TTracker/_apis/wit/workitems/\$User%20Story?api-version=7.1"
```

## Verification checklist for Josh
1. TEST admin Social tab works end to end (approve / reject / edit / reload).
2. To see the 577 alerts on TEST, dispatch the SCOTUS / pardons / EO workflows on `test` (they carry the same `DISCORD_WEBHOOK_URL` secret).
3. Answer the three decisions on 579, 576, 580 and the cron question; then next session cherry-picks 1c356c0 if approved and deploys 572 + 577 to PROD (migration 114 on PROD first, then the edge function, then the code).

## Addendum (later the same evening)

- Closed: ADO-575 (Josh verified the Page post on PROD, screenshot skipped by choice), ADO-576 and ADO-210 (closing stays dead).
- ADO-579: Josh approved the false-premise rubric; shipped to main via PR #142. He still owes the hand-bump numbers for EO 14420 / 14407.
- **ADO-581 created: election interference on the main line.** Verified on PROD that every front is a one-time seed sweep (newest `story_event` row August 25; 427 enriched stories since with no front check). Measured regex recall on 2,413 stories since July 1: seed regex 34, broadened 133, election-word headlines 235, with at least seven real interference stories missed by any headline regex and false positives when summaries are matched. Proposal on the card: hybrid - headline regex as the precision anchor every cycle, plus a Claude fronts-assignment agent (Wave 2 design) judging the election-word candidate pool with a state-action rubric. Per-front `main_line_alarm_floor` (migration 115, election = 3) puts alarm 3+ members on the main line.
- **Josh's instruction for the next session: review the approach first.** He wants election interference items on the main line and is not yet sure the hybrid is the right answer. Open the session with a design review of the 581 comments before building anything, then session 1 (regex backfill + floor migration + pins) once he agrees.

Next-session prompt:
```
/start-work ADO-581: election interference on the main line. Start with a design review of the approach in the card comments (regex recall numbers, the hybrid proposal, the per-front alarm floor) and get Josh's decision before building. Then session 1: regex backfill across all fronts on PROD via the Chrome SQL editor, migration 115 per-front alarm floor, pins for 14648 / 14592 / 14688, refresh, confirm on trumpytracker.com.
```
