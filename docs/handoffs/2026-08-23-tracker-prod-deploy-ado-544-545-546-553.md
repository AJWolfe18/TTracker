# 2026-08-23 — Tracker PROD deploy (ADO-544/545/546) + ADO-553 reopen/fix

## TL;DR

The Tracker shipped to PROD behind `rap_sheet` OFF (PR #126, squash `9a78e80`), migration 111
applied + verified on PROD, and the ADO-553 "overnight enrichment" check FAILED verification in a
useful way: the PROD pardons agent had been running the **v1.0 prompt since May** because the v1.1
prompt commits never left the test branch. That's fixed (prompt + tone-system v2.0 synced to main in
the same PR), the mis-enriched rows were reset, and v1.1 re-enrichment runs were fired the same
night. **Remaining in the morning: Josh eyeballs `?ff_rap_sheet=true`, flag flip commit, close
544/545/546, verify the pardons cohort all reads v1.1 (runs may still be draining), review
needs_review queue.**

## What went to main (PR #126, all checks green, AI review passed)

| Commit (test) | What |
|---|---|
| 95de96c | ADO-544 W1.1 — prerequisite main lacked (timeline.ts, useFeatureFlag, `rap_sheet: false` in flags-prod.json) |
| 7593188 (#117) | ADO-545 Tracker center-spine |
| 47e1acd (#118) | ADO-545 Tracker becomes homepage, stories → /news |
| 81ddf65 | Selection highlight red → steel blue |
| 50c6561 (#119) | ADO-546 fronts data layer (migration 111 + skip-reasons) |
| 807934e (#120) | Dates month-day-year |
| b1e13a5 (#122) | Schema doc fronts tables |
| f9e3afd | **ADO-553: pardons prompt v1.1 + tone-system v2.0 sync to main** (see below) |
| 7e6e10a / 409fb8b | Codex P1: severity vocabulary fix (see below) |
| 33f2a53 / 3e05533 | Codex P2: idempotent identity-sequence grants in migration 111 |
| 29e8a22 / 012b643 | Review P1+P2: branch-agnostic agent Step A + 13 connection-type labels |

Excluded: ADO-551 disposition work (not Ready for Prod), 8c4e7a3 (ephemeral plan doc), all
test-only paths.

## The ADO-553 story (REOPENED, then fixed)

- Expected: 24 backfilled PROD pardons at `prompt_version='v1.1'`. Found: 5 rows at **'v1'**
  (real Claude-agent provenance, wrong prompt), 15+ still pending, and **zero v1.1 rows in PROD
  history ever**.
- Root cause: the PROD trigger (`trig_018LUznaUWwijFhMZLp8kYE2`) hard-resets to `origin/main`,
  and commits 5865910 + e8e580d (v1.1 prompt, tone-system v2.0 binding, May 31) were never
  cherry-picked to main. Every PROD pardon enrichment since May ran the v1.0 prompt, and all three
  PROD agents read tone-system v1.0.0 (no bannedPhrases/bannedPatterns/writingRules).
- Fix: f9e3afd syncs both files; prompt Step A is now branch-agnostic
  (`git rev-parse --abbrev-ref HEAD`) so the file can never redirect a PROD run to test.
- Rows 119–123 (enriched at v1 the same morning) were reset via
  `scripts/maintenance/2026-08-23-ado-553-v1-rows-reset.sql` (test-only path) — Claude ran the
  UPDATE directly in the PROD SQL Editor via the Chrome connection, 5 rows, verified hidden from anon.
- v1.1 re-enrichment: fired manually post-merge. Verified real v1.1 output on PROD: rows 124–128
  enriched at 07:39 UTC with `prompt_version='v1.1'`. Backlog was 19 at the 07:55 run (the original
  Step-2 query caps at limit=20 — the true cohort was larger than the "24" headline). Agent
  processes 5/run (deliberate: research depth vs 15-turn limit), so several sequential runs were
  fired/monitored overnight; the 20:00 UTC cron catches any leftovers.
- Still open on 553: all cohort rows at v1.1 + tone voice + colors (AC3/AC4), the 2 former
  Perplexity-error rows fully researched, needs_review queue (Smith id 120 re-flagged likely,
  Spurlock id 143 already waiting).

## Migration 111 on PROD

Applied by Claude via the browser (Supabase SQL Editor, PROD project) after two false starts:
the grant snippet errored (42P01) because it ran before the migration; and a clipboard handoff
failed because a later chat code block overwrote the clipboard. Final state verified: all four
relations (`events`, `event_updates`, `story_event`, `v_event_stats`) respond via anon PostgREST.
The merged migration file includes the identity-sequence grant DO-block (verified unnecessary on
TEST via live service_role insert — Supabase default privileges cover public-schema sequences;
046 only revoked anon/authenticated — but kept as drift insurance).

## PROD frontend verification (flag override, all passed)

- `/?ff_rap_sheet=true`: masthead tally (580 days / 14,455 developments / 1 alarm-5), spine with
  month markers + alternating sides, filters (All ↔ Alarm 4+ work, counts change 62 → 60-complete),
  chips, search box. Dates "AUG 21, 2026" format.
- Click-through: spine entry → `/detail/14231` full detail page.
- `/news?ff_rap_sheet=true`: Scorecard strip + hero + category/severity pills + cards.
- Flag OFF `/`: byte-identical old homepage, no News tab, no flash.
- Zero console errors; no horizontal overflow (`scrollWidth <= innerWidth` verified).
- NOT verified: real-mobile layout (window resize was ignored — maximized). Josh's morning pass.

## Review findings verified this session (worth remembering)

- Codex P1 (real): timeline.ts had its own severity map (`serious/notable/watch/win`) that never
  matched the DB (`critical/severe/moderate/minor`); moderate rows vanished from Alarm 3+. Fixed by
  importing the canonical map from adapter.ts and deriving the server predicate from it.
- Codex P2 (false positive): service_role sequence grants — proven unnecessary by live insert; grant
  added anyway as no-op insurance.
- Review P2 (real, wider than reported): `wealthy_unknown`, `cabinet_connection`, `lobbyist` all
  missing from React CONNECTION_LABELS + filters dropdown (vanilla pardons-app.js had them).
  Regression tests now pin all 13 DB-allowed values.
- First-round review false positives: `wealthy_unknown` "not in enum" (migration 062 added it),
  Jan 6 id=3 hardcode (protected by `prompt_version='locked'`), tone-system "To be fair"
  contradiction (both entries ban it).

## Morning checklist (Josh)

1. Eyeball `https://trumpytracker.com/?ff_rap_sheet=true` (desktop + real mobile).
2. Say the word → flag flip (`rap_sheet: true` in flags-prod.json) as its own commit/PR to main.
3. Close ADO-544/545/546 (AC verification against the cards before closing).
4. Check pardons cohort: all rows v1.1, colors on /pardons, 2 ex-Perplexity rows researched → then
   close ADO-553. Review admin needs_review queue (Smith, Spurlock, likely more L1/L3 flags).
5. Optional: delete `PERPLEXITY_API_KEY` GitHub secret (zero consumers since #124).
