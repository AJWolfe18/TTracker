# SCOTUS Go-Live — Session Handoff (Feb 23)

**ADO:** 80, 81, 82, 354 all Closed.

PR #80 (frontend, 11 files) merged to main. PR #81 (flag flip `scotus: true` + P2 fixes from #80 review) merged to main. SCOTUS tab is now live on trumpytracker.com with 51 public merits cases. P2 fixes applied to both main and test: pardons timeline analytics tracking + admin enrichment timeout reset. ADO-390 remains open for enrichment gaps (108 unenriched + 7 merits not public) — separate session.
