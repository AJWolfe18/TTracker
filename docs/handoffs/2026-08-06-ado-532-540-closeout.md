# 2026-08-06 — ADO-540 verified+closed, ADO-532 eval harness shipped to main+closed

**Tickets:** ADO-540 (Closed) · ADO-532 (Closed)
**Executed from:** handoff `2026-08-05-ado-537-prod-deploy-complete.md` "Next session" queue, items 1 + the 540 verification gate.

## ADO-540 — EO raw-only verification (Closed)

The 2026-08-05 scheduled `executive-orders-tracker.yml` run (31031221599, 17:41 UTC, main)
completed **green with zero OpenAI calls** — the raw-only script's "AI enrichment runs as
separate workflow step" note is in the log, no GPT activity anywhere. No raw rows were written
only because the Federal Register published **zero new EOs Aug 2–5** (`count: 0`); the write
path was proven on TEST pre-PR #110, and the run verified PROD DB connectivity (266 records,
order range 14145–14416). Evidence comment on the card; closed 02:56 UTC.
`OPENAI_API_KEY`'s sole remaining consumer is AI PR code review.

## ADO-532 — eval harness + gold set to main via PR #111 (Closed)

**PR #111 squash-merged to main = `6032ec1`.** Cherry-picked onto `deploy/ado-532-eval-harness`:
- `aed01aa` — harness (`scripts/evals/`: clustering-eval.js, run-eval.js, eval-types.js,
  build-clustering-gold-set.js), gold set JSON, log extracts, `qa:clustering-eval` in `qa:smoke`,
  3 `export` keywords in hybrid-clustering.js (only live-code touch, zero behavior change).
- `653118a` — Josh's spot-check verification (196 pairs after 2 flips + 2 removals).
- `bc433a2` — ADO-537's stranded docs (prod-deployment-manifest + database-schema: judge tables).

**Conflict resolutions** (all docs, took picked side): `ai-evals/plan.md` (absent on main),
`clustering-quality/plan.md` + `database-schema.md` (main's copies stale). The known
`backfill-plan.md` gotcha did NOT apply — none of these commits touch it.

**Review hardening during the PR** (both fixes cherry-picked back to test as `8927d41` + `0040970`):
1. **Codex P1 × 2, both verified real:** builder could overwrite the published gold set via
   `--out` (header promised it never would); eval silently dropped unknown `--ids` producing
   misleading metrics. Fixed: hard refusal + loud failure listing missing ids.
2. **AI-review blocker on the first fix, also real:** plain `resolve()` equality misses
   case-variant paths on Windows (Josh's machine). Guard now realpaths + folds case on win32.
   Verified: exact and CASE-VARIANT paths both refused, legit draft paths pass.

**Validation:** full `qa:smoke` green on the deploy branch; fidelity test (100% replay-vs-live)
green on deploy branch AND on test after the cherry-pick-back. Final AI review: zero blockers.

**Why this mattered:** the PROD Judge prompt referenced a gold-set file that didn't exist on
main (only `judge-dryrun.js` did), and ADO-539's calibration gate (precision holds ≥98% before
any prompt change ships) needs the harness runnable from main. Both now resolved.

## Next session (in order)

1. **ADO-539 — Judge calibration v1.1.** Analysis is on the card (from the 2026-08-05
   uncertain-verdict review): re-hedging (no verdict memory — Gaza pair judged uncertain 6×),
   "B lacks specifics" hedges against same-cycle commentary Section 4 already licenses, and
   format-variant hedges. Confidences cluster 0.35–0.5 → rule changes, not threshold changes.
   **Hard gate: run the (now-on-main) gold-set eval; precision must hold ≥98% before shipping.**
2. **ADO-531 — backfill.** Plan approved at `docs/features/clustering-judge/backfill-plan.md`
   — execute, don't re-plan.

## Loose ends (unchanged from prior handoff)

- ADO-515 social share: still blocked on Josh setting Netlify PROD env vars.
- ADO cards 447/510 sit in Testing but look WhiskeyPal-domain, not TrumpyTracker.
- PROD DB size over free cap (Mercury/upgrade decision still open, no ticket).

**Commits — test:** `8927d41`, `0040970` (Codex/review fixes synced back). **Main:** `6032ec1` (PR #111).
