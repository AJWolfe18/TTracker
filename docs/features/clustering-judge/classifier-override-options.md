# Moved

This doc is now the general playbook: `docs/reference/cloud-classifier-playbook.md` (what to do when the cloud auto-mode classifier denies any routine; the Judge case, ADO-583, is its worked example).
